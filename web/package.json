{
  "name": "microfrontends",
  "private": true,
  "workspaces": [
    "navbar",
    "main",
    "root-config"
  ],
  "scripts": {
    "start": "wsrun --parallel start",
    "build": "yarn workspaces run build"
  },
  "devDependencies": {
    "wsrun": "^5.2.0"
  }
}
